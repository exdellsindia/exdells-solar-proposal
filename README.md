# exdells-solar-proposal

Production-ready, print-optimized solar proposal generator for EXDELLS.

## Quick start

```bash
npm install
npm run render
```

The PDF will be generated at `exdells-solar-proposal.pdf`.

## Optional preview

```bash
npm run serve
```

Open http://localhost:3000 to preview the HTML layout before exporting.
